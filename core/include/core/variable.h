#pragma once

#include "core/type.h"

namespace core
{
    GLvoid xy();         // xy
    GLvoid xyz();        // xy z
    GLvoid xyrgb();      // xy   rgb
    GLvoid xyzrgb();     // xy z rgb
    GLvoid xyrgba();     // xy   rgb a
    GLvoid xyzrgba();    // xy z rgb a
    GLvoid xyrgbi();     // xy   rgbi
    GLvoid xyzrgbi();    // xy z rgbi
    GLvoid xyrgbai();    // xy   rgb ai
    GLvoid xyzrgbai();   // xy z rgb ai
    GLvoid xyst();       // xy          st
    GLvoid xyzst();      // xy z        st
    GLvoid xyrgbst();    // xy   rgb    st
    GLvoid xyzrgbst();   // xy z rgb    st
    GLvoid xyrgbast();   // xy   rgb a  st
    GLvoid xyzrgbast();  // xy z rgb a  st
    GLvoid xyrgbist();   // xy   rgbi   st
    GLvoid xyzrgbist();  // xy z rgbi   st
    GLvoid xyrgbaist();  // xy   rgb ai st
    GLvoid xyzrgbaist(); // xy z rgb ai st
}